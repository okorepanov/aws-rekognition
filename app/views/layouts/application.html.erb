<!DOCTYPE html>
<html>
  <head>
    <title>Aws Rekognition</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <header class="bg-gradient-to-r from-gray-950 to-gray-500 font-semibold italic p-4">
    <div class="container mx-auto flex justify-between items-center">
      <h1 class="text-2xl text-white hover:underline">
        <%= link_to "Aws Rekognition", root_path %>
      </h1>
      <div>
        <%= render 'layouts/search' %>
      </div>
      <nav class="float-right text-white">
        <%= render 'layouts/navigation' %>
      </nav>
    </div>
  </header>

  <body>
    <div class="container mx-auto mt-4">
      <%= yield %>
    </div>
  </body>
</html>
